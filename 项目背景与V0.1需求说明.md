# 项目背景与需求说明

> 用途：作为新会话 / ChatGPT Work 模式中的项目启动上下文与第一版开发需求说明。  
> 当前阶段：概念验证（PoC）→ V0.1 MVP  
> 日期：2026-09-18

---

## 1. 项目背景

当前希望构建一套能够**长时间、低人工干预地自主完成软件开发任务**的 Coding Agent 系统。

核心动机不是再做一个“会写代码的聊天机器人”，也不是重新实现一套 Claude Code / Codex / OpenCode，而是解决下面这个更具体的问题：

> **如何让本地大模型承担绝大多数低成本、机械性的软件工程工作，只在真正困难的任务上调用昂贵的云端前沿模型，同时保证任务能够持续执行、自动验证、失败恢复和最终验收。**

当前本地环境具备运行较大本地模型的条件：

- Windows 10 / 11 开发环境
- Intel i9-13980HX
- NVIDIA RTX 4060 Laptop 8GB VRAM
- 64GB RAM
- Ollama
- 当前重点本地模型：`glm-4.7-flash:q4_K_M`
- 该模型约 19GB，无法全部放入 8GB 显存，需要 CPU + GPU / RAM 混合推理
- 本地模型适合承担：
  - 读文件
  - 改文件
  - CRUD
  - 编写测试
  - 执行命令
  - 读取日志
  - 简单 Debug
  - Git diff
  - 文档整理
  - 结构化输出
  - 一般 Coding Agent 工具调用
- 云端前沿模型（例如 GPT-5.6 Sol）适合承担：
  - 初始架构设计
  - 复杂问题分析
  - 高难 Debug
  - Milestone Review
  - 最终验收
  - 本地模型多次失败后的专家升级

项目的核心思路可以概括为：

> **高级模型负责智力密度高的工作，本地模型负责大量机械 Token。**

---

# 2. 项目定位

推荐定位：

## Local-First Hybrid Coding Supervisor

中文可理解为：

> **本地优先、成本感知、支持云端专家升级的自主软件工程监督器。**

它本身不需要成为新的 Coding Agent Runtime，而应尽量复用已有成熟执行底座。

推荐分工：

```text
Codex
= Coding Agent Runtime

Ollama
= Local LLM Runtime

GLM / Qwen 等本地模型
= Local Worker

GPT-5.6 Sol / 其他前沿模型
= Cloud Expert

Git
= 工程状态与可恢复记忆

pytest / Maven / npm test / build / health check
= Deterministic Verifier

本项目
= Supervisor + Router + State + Retry + Escalation + Acceptance
```

核心卖点不是：

> Autonomous Coding Agent

而应该是：

> **Use local models for most coding work, escalate only the hard parts to frontier models.**

即：

> **让本地模型一直干活，只有必要时才花昂贵模型 Token。**

---

# 3. 与现有开源项目的关系

GitHub 上已经存在很多类似方向，因此本项目不能宣传为“世界第一个 Autonomous Coding Agent”。

已存在的相关方向包括：

- Ralph / Ralph Loop
  - 目标驱动
  - 多轮任务执行
  - Fresh Context
  - Git / PRD / progress 文件持久化
- ralph-code
  - Planning / Execution 模型分离
  - 自动拆任务
  - Retry
  - Codex / Claude 等组合
- OpenHands
  - 完整软件开发 Agent
  - Shell / 文件 / 浏览器 / 本地模型
- SWE-agent
  - 自动解决 GitHub Issue
- 本地 Ollama Coding Agent
  - 本地模型 + Shell + 文件 + Git
- 一些 Hybrid Router 项目
  - Local-first
  - Cloud fallback
  - 成本统计
  - Escalation

因此本项目真正应该追求的差异化是：

1. **Codex-native**
2. **Local-first，但不是 Local-only**
3. **Capability Escalation，而不是基础设施 fallback**
4. **确定性验收 Gate**
5. **任务级状态持久化与断点恢复**
6. **对普通消费级 PC 友好，尤其 Windows**
7. **透明统计本地覆盖率、云端依赖率、Token 与耗时**
8. **本地模型失败后，自动将最小必要上下文升级给云端专家**
9. **云端模型给出解决方案后，再交还本地 Worker 继续执行**
10. **长期目标：形成真实世界 Local Coding Model Benchmark**

---

# 4. 核心产品目标

用户只需要输入一个最终目标，例如：

```text
完成 Roco Poam MVP：
用户可以搜索已有歌曲，
将 MIDI 转换为洛克王国世界游戏琴谱，
并进行试听。
要求项目可以本地启动，
核心功能有自动测试，
SQLite 初始化正常。
```

之后系统应尽量无人值守地完成：

```text
Goal
 ↓
Planning
 ↓
Task Decomposition
 ↓
Local Worker
 ↓
Code / Shell / Test / Git
 ↓
Verifier
 ↓
PASS ? ────── YES ───→ Next Task
  │
  NO
  ↓
Local Retry
  ↓
达到失败阈值？
  │
  YES
  ↓
Cloud Expert Escalation
  ↓
生成诊断 / 解决方案
  ↓
交还 Local Worker
  ↓
继续执行
  ↓
最终 Acceptance
  ↓
DONE
```

理想体验：

> 用户晚上输入一个目标，第二天回来查看：
>
> - 完成后的 Git branch / commit
> - 自动测试结果
> - 变更摘要
> - FINAL_REPORT.md
> - 本地模型完成比例
> - 云端专家调用次数
> - Token 消耗
> - 总运行时长
> - 是否需要人工介入

---

# 5. 核心架构

推荐第一版架构：

```text
                         USER
                          │
                          │ Goal
                          ▼
                ┌───────────────────┐
                │    Supervisor     │
                │                   │
                │ 状态 / 调度 / 停止 │
                └─────────┬─────────┘
                          │
                          ▼
                   ┌────────────┐
                   │  Planner   │
                   └─────┬──────┘
                         │
                     TASKS.json
                         │
                         ▼
                   ┌────────────┐
                   │   Router   │
                   └─────┬──────┘
                         │
              ┌──────────┴──────────┐
              ▼                     ▼
        Local Worker            Cloud Expert
       Ollama + GLM             GPT-5.6 Sol
              │                     │
              └──────────┬──────────┘
                         ▼
                     Codex Exec
                         │
               ┌─────────┼──────────┐
               ▼         ▼          ▼
             Files     Shell       Git
                         │
                         ▼
                    Test / Build
                         │
                         ▼
                 Deterministic Gate
                         │
               ┌─────────┴──────────┐
               ▼                    ▼
             PASS                  FAIL
               │                    │
               ▼                    ▼
           Next Task            Retry Local
                                    │
                              达到阈值后升级
                                    │
                                    ▼
                              Cloud Expert
```

---

# 6. 为什么选择 Codex 作为执行底座

本项目第一版**不应该自己重写 Coding Agent 的底层能力**。

不建议自行实现：

- 文件读取
- 文件修改
- patch
- shell 执行
- grep
- Git
- Agent Loop
- 工具调用协议
- 上下文管理
- Coding Agent 沙盒

优先复用：

```bash
codex exec ...
```

Codex 负责：

- Agent Loop
- 文件操作
- Shell
- Git
- 上下文
- 工具调用
- Coding 执行

Supervisor 只负责：

- 目标管理
- Task 管理
- Worker 选择
- Retry
- Escalation
- Verification
- State
- Resume
- Stop Condition
- Metrics

这样可以极大缩小项目复杂度。

---

# 7. 本地模型策略

第一阶段默认本地 Worker：

```text
glm-4.7-flash:q4_K_M
```

当前硬件：

```text
RTX 4060 Laptop 8GB
64GB RAM
```

模型约 19GB，因此需要考虑：

- CPU / GPU 混合推理
- 上下文越大，KV Cache 与 Prompt Eval 成本越高
- 不追求模型最大理论上下文
- 更应该追求真实吞吐的 Sweet Spot

初步建议测试：

- 16K
- 24K
- 32K
- 48K

不建议一开始就使用 128K / 200K 上下文。

---

# 8. 上下文管理原则

长期自主 Agent 不应该依赖“一个永远不结束的超长 Chat”。

推荐：

> **一个 Task = 一个相对独立的 Codex Session**

长期记忆通过文件与 Git 持久化。

例如：

```text
Task 001
↓
Codex Session A
↓
完成
↓
保存状态 / commit

Task 002
↓
Codex Session B
↓
重新读取 GOAL / STATE / TASKS / Git
↓
继续
```

这样即使发生：

- Context 被压缩
- Codex Thread 崩溃
- Windows 重启
- Ollama 重启
- Supervisor 重启

也能够恢复。

---

# 9. 项目状态目录设计

建议每个被管理项目中建立：

```text
.agent/
├── GOAL.md
├── PLAN.md
├── ACCEPTANCE.md
├── TASKS.json
├── STATE.json
│
├── memory/
│   ├── architecture.md
│   ├── decisions.md
│   ├── problems.md
│   └── lessons.md
│
├── metrics/
│   ├── runs.jsonl
│   ├── tokens.jsonl
│   └── summary.json
│
└── logs/
    ├── supervisor.log
    ├── worker.log
    ├── verifier.log
    └── escalation.log
```

另外项目根目录可包含：

```text
AGENTS.md
```

用于定义项目规则、命令、代码规范和 Agent 行为约束。

---

# 10. STATE.json 建议字段

示例：

```json
{
  "project": "Roco-Poam",
  "status": "running",
  "current_task_id": "T006",
  "completed_tasks": [
    "T001",
    "T002",
    "T003",
    "T004",
    "T005"
  ],
  "failed_tasks": [],
  "last_update": "2026-09-18T21:00:00+08:00",
  "local_attempts": 2,
  "cloud_escalations": 1,
  "human_interventions": 0
}
```

---

# 11. TASKS.json 建议结构

示例：

```json
{
  "tasks": [
    {
      "id": "T001",
      "title": "检查现有项目结构",
      "status": "done",
      "depends_on": [],
      "attempts": 1
    },
    {
      "id": "T002",
      "title": "实现 SQLite 初始化",
      "status": "done",
      "depends_on": ["T001"],
      "attempts": 1
    },
    {
      "id": "T003",
      "title": "实现歌曲搜索",
      "status": "running",
      "depends_on": ["T002"],
      "attempts": 2
    }
  ]
}
```

第一版不需要过度复杂的 DAG。

---

# 12. Retry 策略

建议第一版：

```text
Local Worker
↓
失败
↓
分析测试 / 日志
↓
Local Retry
↓
失败
↓
Local Retry
↓
失败
↓
达到 max_local_attempts
↓
Cloud Escalation
```

默认：

```yaml
max_local_attempts: 3
```

必须避免：

```text
GLM 同一个错误重复尝试几十次
```

---

# 13. Escalation 机制

Cloud Expert 不应该每一步都参与。

应当只在以下情况触发：

### 条件 A：本地连续失败

```text
attempts >= 3
```

### 条件 B：任务被分类为高难

例如：

- 大规模架构调整
- 复杂并发
- 数据一致性
- 多服务事务
- 本地模型多次生成错误 patch
- 测试失败根因始终无法定位

### 条件 C：最终 Milestone Review

例如：

- 数据层完成
- API 层完成
- 核心业务闭环完成
- Release 前

---

# 14. Escalation 输入必须最小化

不应该把整个几十万 Token 的历史发送给云端。

应整理成：

```text
GOAL
+
当前 Task
+
相关源码
+
git diff
+
测试失败
+
关键日志
+
本地模型尝试摘要
+
已知约束
```

例如：

```json
{
  "task": "修复 MIDI tempo change 转换错误",
  "local_attempts": 3,
  "local_tokens": 82341,
  "elapsed_seconds": 1320,
  "errors": [
    "tempo conversion mismatch"
  ],
  "worker_summary": "...",
  "relevant_files": [
    "app/services/midi_parser.py",
    "tests/test_midi_parser.py"
  ]
}
```

云端专家输出：

```text
诊断
+
建议修改方案
+
必要 patch / 伪代码
+
新增验证点
```

之后优先交还本地 Worker 执行。

---

# 15. Verifier 必须尽量确定性

这是整个项目非常重要的原则：

> **模型说“我完成了”不能作为任务完成依据。**

应该尽量使用机器可验证信号。

例如：

```text
pytest exit code == 0

mvn test exit code == 0

npm test exit code == 0

npm run build exit code == 0

GET /health == 200

SQLite table exists

API response matches schema

CLI command exits 0
```

因此：

```text
Agent 自评
≠
Done
```

应该是：

```text
Acceptance Gate PASS
=
Done
```

---

# 16. Stop Condition

只有以下条件全部满足时才能进入 `DONE`：

1. 所有必须 Task 完成
2. Acceptance 全部通过
3. 测试全部通过
4. Build 通过
5. 无 Blocker 级未解决问题
6. Final Review 通过
7. 生成 FINAL_REPORT.md

若达到安全限制或资源限制，应进入：

```text
BLOCKED
```

而不是伪装成 DONE。

---

# 17. 权限与安全策略

无人值守 Agent 不能直接拥有无限系统权限。

推荐默认权限：

| 操作 | 默认 |
|---|---|
| 读取项目目录 | 自动 |
| 修改项目目录 | 自动 |
| 创建项目文件 | 自动 |
| 执行 pytest | 自动 |
| Maven / npm / Python 命令 | 自动 |
| Git diff / status | 自动 |
| 本地 git commit | 可配置 |
| 下载依赖 | 白名单 / 可配置 |
| 删除项目目录内生成文件 | 受限 |
| 修改项目外文件 | 禁止 |
| 修改 Windows 系统配置 | 禁止 |
| 删除外部数据库 | 禁止 |
| 访问生产环境 | 禁止 |
| git push | 默认禁止 |
| 云服务器部署 | 默认禁止 |
| 使用危险 bypass sandbox | 默认禁止 |

核心原则：

> **项目目录里有相对自由，项目目录外严格限制。**

---

# 18. V0.1 范围

第一版不要做成大型 Agent 平台。

建议 V0.1 只完成以下能力：

## 18.1 CLI

例如：

```bash
foreman run "完成 Roco Poam MVP"
```

也可以支持：

```bash
foreman run --goal GOAL.md
```

---

## 18.2 任务初始化

自动生成：

```text
.agent/GOAL.md
.agent/PLAN.md
.agent/ACCEPTANCE.md
.agent/TASKS.json
.agent/STATE.json
```

---

## 18.3 本地 Worker

通过 Codex CLI + Ollama 调用本地模型。

目标形态：

```text
Supervisor
↓
codex exec
↓
Ollama
↓
GLM
```

---

## 18.4 Verifier

至少支持：

- 任意 shell command
- pytest
- build command
- exit code
- 简单 HTTP health check

V0.1 不需要复杂 AI Judge。

---

## 18.5 Retry

默认：

```text
max_local_attempts = 3
```

---

## 18.6 Cloud Escalation

第一版可以只支持一个 Cloud Expert。

例如：

```text
GPT-5.6 Sol
```

后续再做多 Provider。

---

## 18.7 Resume

支持：

```bash
foreman resume
```

读取：

```text
STATE.json
TASKS.json
Git
```

继续运行。

---

## 18.8 Final Report

最终生成：

```text
FINAL_REPORT.md
```

必须包含：

- 最终状态
- 完成任务
- 未完成任务
- 测试结果
- Build 结果
- 变更文件
- Git commit
- 本地模型调用次数
- 本地 Token
- 云端 Token
- Cloud Escalation 次数
- 总耗时
- 人工介入次数
- 已知问题

---

# 19. 第一版暂时不做的功能

V0.1 不建议引入：

- LangChain
- LangGraph
- CrewAI
- 多 Agent 群聊
- 向量数据库
- RAG
- Web UI
- 浏览器自动化
- Kubernetes
- 分布式执行
- 多机 Worker
- 自动生产部署
- 大型权限系统
- 复杂 DAG Scheduler
- LLM-as-a-Judge 主验收
- 自动修改系统配置

优先保持：

> Python + Codex CLI + Ollama + Git + 测试工具

---

# 20. 推荐技术栈

第一版：

```text
Python 3.12+
Typer / argparse
Pydantic
subprocess / asyncio
JSON / JSONL
Git CLI
Codex CLI
Ollama
pytest
httpx
```

可选：

```text
Rich
```

用于 CLI 状态展示。

数据层第一版甚至不一定需要数据库。

优先：

```text
JSON
JSONL
Markdown
Git
```

足够。

---

# 21. 推荐项目结构

```text
local-foreman/
│
├── pyproject.toml
├── README.md
├── LICENSE
│
├── src/
│   └── foreman/
│       ├── cli.py
│       ├── supervisor.py
│       ├── planner.py
│       ├── router.py
│       ├── worker.py
│       ├── verifier.py
│       ├── escalation.py
│       ├── state.py
│       ├── metrics.py
│       └── models.py
│
├── prompts/
│   ├── planner.md
│   ├── worker.md
│   ├── reviewer.md
│   └── escalation.md
│
├── tests/
│
└── examples/
    └── roco-poam/
```

---

# 22. 核心抽象

建议最小抽象：

```python
class Task:
    id: str
    title: str
    status: str
    attempts: int


class Worker:
    def run(self, task: Task) -> RunResult:
        ...


class Verifier:
    def verify(self, task: Task) -> VerifyResult:
        ...


class EscalationProvider:
    def diagnose(self, context: EscalationContext) -> ExpertAdvice:
        ...


class Supervisor:
    def run(self):
        ...
```

避免一开始设计过度复杂的插件系统。

---

# 23. Supervisor 主循环

核心逻辑可以非常简单：

```python
while not project_done:

    task = get_next_task()

    result = local_worker.run(task)

    verification = verifier.verify(task)

    if verification.passed:
        mark_done(task)
        checkpoint()
        continue

    task.attempts += 1

    if task.attempts < max_local_attempts:
        save_failure_context()
        continue

    advice = cloud_expert.diagnose(
        build_minimal_escalation_context(task)
    )

    save_expert_advice(advice)

    local_worker.run_fix(task, advice)

    verification = verifier.verify(task)

    if verification.passed:
        mark_done(task)
    else:
        mark_blocked_or_replan(task)

    checkpoint()
```

---

# 24. 成本感知与 Metrics

这是本项目的重要差异化功能。

每次运行记录：

```json
{
  "task": "Implement MIDI parser",
  "worker": "glm-4.7-flash:q4_K_M",
  "local_tokens": 138422,
  "cloud_tokens": 7221,
  "local_attempts": 3,
  "elapsed_seconds": 1267,
  "cloud_escalated": true,
  "result": "PASS"
}
```

---

# 25. 关键指标

## 25.1 Local Coverage Rate

定义：

```text
无需云端模型帮助成功完成的任务数
/
总任务数
```

例如：

```text
27 / 31 = 87.1%
```

这是项目非常重要的指标。

---

## 25.2 Frontier Model Dependency

```text
需要云模型介入的任务数
/
总任务数
```

---

## 25.3 Cloud Token Ratio

```text
Cloud Tokens
/
Total LLM Tokens
```

---

## 25.4 Human Intervention Count

真正无人值守的关键指标：

```text
Human interventions = 0
```

---

## 25.5 Autonomous Runtime

例如：

```text
7h 21m
```

---

# 26. 未来 Benchmark 方向

长期可以把本项目发展成本地 Coding 模型的真实 Agent Benchmark。

例如同一个任务集：

```text
RTX 4060 + GLM
Local Coverage 72%

RTX 5090 + Qwen Coder
Local Coverage 91%

Mac Studio + 120B
Local Coverage 96%
```

比传统 HumanEval 更贴近真实开发：

> **这个模型到底可以自己完成多少真实软件工程任务？**

未来可以形成：

- Local Coverage 排行
- Hardware / Model 排行
- Cost / Task
- Tokens / Task
- Time / Task
- Escalation Rate
- Test Pass Rate

---

# 27. Windows 必须是一等公民

项目应从第一天就测试：

```text
Windows 10 / Windows 11
PowerShell
Ollama
Codex CLI
RTX 4060 8GB
64GB RAM
```

不要只在 Linux / macOS 下工作。

需要特别处理：

- PowerShell quoting
- Windows path
- subprocess
- UTF-8
- CRLF
- Python executable
- npm / Maven
- Codex CLI executable detection
- Ollama 服务检测

---

# 28. 第一场真实实验：Roco Poam

Roco Poam 是当前另一个正在规划的 MVP 项目，可作为 Foreman 的第一个真实 Benchmark。

## Roco 背景

目标：

> 为《洛克王国：世界》玩家提供游戏琴谱。

用户核心体验：

```text
搜索歌曲
↓
生成游戏琴谱
↓
试听
```

第一版目标：

- 最短时间上线
- 中国热门歌曲优先
- 不要求用户上传文件即可使用
- 后端可以使用纯 Python
- 数据库优先 SQLite
- 不追求大型架构
- 优先 MVP

---

# 29. Roco MVP 验收目标

可以把下面目标作为 Autonomous Supervisor 的首个真实测试。

## Goal

```text
完成 Roco Poam MVP，使项目可以在本地稳定运行。
用户能够搜索数据库中的歌曲，
将 MIDI 数据转换为洛克王国世界游戏琴谱，
并能够进行试听。
```

## Acceptance

### 后端

- [ ] FastAPI 可以正常启动
- [ ] SQLite 自动初始化
- [ ] 歌曲表能够正常读写
- [ ] 提供歌曲搜索 API
- [ ] 支持读取 / 解析 MIDI
- [ ] MIDI 能转换为目标游戏谱格式
- [ ] 返回结构化谱面结果
- [ ] 核心接口有 pytest
- [ ] pytest 全部通过

### 前端 / Web

若当前仓库包含前端：

- [ ] 项目可启动
- [ ] 可以搜索歌曲
- [ ] 可以显示谱面
- [ ] 可以试听
- [ ] build 通过

### 工程

- [ ] README 包含从零启动步骤
- [ ] `.env.example` 完整
- [ ] 无明显敏感信息
- [ ] Git 工作区最终状态明确
- [ ] FINAL_REPORT.md 生成

---

# 30. 理想的 Roco 自主运行过程

目标体验：

```text
21:00
用户输入目标

21:02
Planner 完成

21:10
项目扫描完成

21:30
SQLite 完成

22:00
搜索 API 完成

22:45
MIDI parser 第一次失败

23:00
Local Retry

23:20
Local Retry

23:40
第三次失败

23:41
Escalate GPT-5.6

23:45
专家诊断返回

00:10
GLM 按建议修复

00:20
pytest PASS

01:00
谱面转换完成

02:00
试听功能完成

03:00
全量测试

03:20
Final Review

03:40
README 修复

04:00
Clean Start Test

04:10
DONE
```

第二天只需要查看：

```text
git diff
FINAL_REPORT.md
metrics summary
```

---

# 31. 最终报告示例

理想输出：

```text
Roco Poam Autonomous Build
──────────────────────────────

Status:
DONE

Tasks:
31

Completed locally:
27

Cloud escalations:
4

Local Coverage:
87.1%

Local tokens:
2,381,442

Cloud tokens:
118,204

Cloud Token Ratio:
4.7%

Human interventions:
0

Runtime:
7h 21m

Tests:
51 passed

Build:
PASS
```

---

# 32. V0.1 成功标准

Foreman V0.1 不要求能够完全自主开发任意项目。

只需要证明下面这条链路真实可用：

```text
Goal
↓
自动任务拆解
↓
GLM + Codex 自主开发
↓
自动测试
↓
失败重试
↓
多次失败后 Cloud Escalation
↓
GLM 根据专家建议继续
↓
Acceptance
↓
DONE
```

并至少在 Roco 项目上完成一次真实运行。

---

# 33. V0.1 开发优先级

建议严格按以下顺序：

## P0

1. Codex CLI 可被 Python 稳定调用
2. Codex 可以通过 Ollama 使用 GLM
3. Supervisor 可以运行一个任务
4. Verifier 可以执行 pytest / shell command
5. STATE 可以写入和恢复

## P1

6. TASKS 自动管理
7. Retry
8. Cloud Escalation
9. Resume
10. Final Report

## P2

11. Token / 时间统计
12. Local Coverage
13. CLI 美化
14. Windows 兼容
15. Roco Benchmark

## P3

以后再做：

- 多 Local Model Router
- 多 Cloud Provider
- Benchmark Suite
- Web UI
- GitHub Issue 自动监听
- 定时无人值守开发
- PR 自动创建
- CI 集成
- Docker Sandbox
- 远程 Worker

---

# 34. 设计原则

开发过程中必须坚持：

### 原则 1

**不要重新造 Codex 已经解决的轮子。**

### 原则 2

**确定性工具优先于 LLM 判断。**

### 原则 3

**状态存文件，不把 Chat Context 当数据库。**

### 原则 4

**Local-first，而不是强行 Local-only。**

### 原则 5

**失败应该升级，不应该无限重复。**

### 原则 6

**Cloud Expert 输入最小化。**

### 原则 7

**先做能够运行的 1000～2000 行核心工具，再考虑平台化。**

### 原则 8

**Windows 从 V0.1 就必须能用。**

### 原则 9

**每项能力都应服务于“无人值守完成任务”，不要为了 Agent 概念堆框架。**

### 原则 10

**项目的价值用结果衡量：完成率、Local Coverage、Cloud Token、人工介入次数。**

---

# 35. 第一阶段建议名称

暂时可用工作名：

```text
Talpo
```

含义：

```text
Local Worker = 工人
Cloud Expert = 专家
Foreman = 工头 / 调度者
```

但正式开源前需要检查 GitHub / PyPI 名称冲突。

备选：

- CodeForeman
- AgentForeman
- LLMForeman
- Talpo
- HybridForeman

名称不是当前阻塞项。

---

# 36. 新会话 / Work 模式的开发指令建议

在新的 Work 会话中，可直接将本文件作为背景，并给出类似指令：

```text
请基于这份需求文档开始开发 Talpo V0.1。

要求：

1. 不要一次做大而全。
2. 先完成可运行的最小闭环。
3. 优先 Windows + Python 3.12。
4. 第一阶段只使用 Python + Codex CLI + Ollama + Git。
5. 不引入 LangChain / LangGraph / CrewAI。
6. 先实现：
   - 项目初始化
   - STATE
   - TASKS
   - Codex Worker
   - Verifier
   - Retry
7. 每完成一个阶段都运行测试。
8. 任何架构调整都要保持简单。
9. 最终目标是用 Roco Poam 作为第一个真实无人值守 Benchmark。
10. 所有设计都围绕：
    Local-first + deterministic verification + cloud escalation。
```

---

# 37. 当前项目愿景

最终希望达到：

> **一个普通开发者，只拥有一台消费级 PC 和一个本地模型，也可以让 AI 在夜间自主进行数小时软件开发；大部分 Token 完全在本地消耗，只有真正困难的少量问题才升级到前沿云模型。**

理想体验：

```text
晚上：

foreman run "实现 Issue #32"

↓

睡觉

↓

第二天：

branch ready
tests passed
report generated
```

并能够清晰回答：

```text
这个项目 AI 一共干了多久？

多少任务本地完成？

哪些问题必须调用 GPT？

本地模型用了多少 Token？

云模型用了多少 Token？

省了多少昂贵模型调用？

人工介入了几次？

最终测试是否真的通过？
```

---

# 38. 一句话定义

> **Talpo 是一个 Local-first、Codex-native、支持确定性验收与云端专家升级的自主软件工程 Supervisor，让本地大模型承担绝大多数开发工作，只在必要时调用前沿模型。**

