# 工作记录

每个开发会话创建一个文件：

```text
docs/worklog/YYYYMMDD-<topic>.md
```

记录必须回答：

- 本次目标是什么？
- 依据了哪些已有决策？
- 实际改了什么？
- 执行了哪些验证命令？结果是什么？
- 哪些事项仍未验证、失败或被阻塞？
- 下一次会话从哪里继续？

工作记录是事实日志，不是计划清单。计划使用 `docs/ROADMAP.md`，架构取舍使用 `docs/decisions/`。

