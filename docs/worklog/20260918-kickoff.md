# 2026-09-18：Talpo 项目启动

## 目标

建立 Talpo V0.1 的开发规范、架构边界、路线门槛和跨会话记录方式，为执行链路 Spike 做准备。

## 前置状态

- 当前仓库只有《项目背景与V0.1需求说明.md》，尚无实现代码、测试或 Git 提交。
- 项目阶段为 PoC → V0.1 MVP。
- 目标闭环为 Local Worker、确定性 Verifier、有限重试、云端升级、恢复和最终报告。

## 实际工作

- 新增根目录 `AGENTS.md`，定义项目定位、V0.1 边界、安全规则和会话工作方式。
- 新增 `docs/ARCHITECTURE.md`，确定 Supervisor、Worker、Verifier、State Store 和 Escalation 的最小边界。
- 新增 `docs/ROADMAP.md`，定义 Gate 0 到 Gate 3 的验收门槛。
- 新增 `docs/decisions/ADR-0001-supervisor-boundary.md`，记录不重写 Agent Runtime 的架构决策。
- 新增 `docs/worklog/` 模板和记录约定。

## 验证

| 命令 | 结果 | 证据/限制 |
|---|---|---|
| `rg --files` | PASS | 确认原始仓库只有需求说明文件 |
| 文档审阅 | PASS | 规范与现有 V0.1 目标保持一致 |

## 未完成与风险

- 尚未验证当前机器上的 Codex CLI、Ollama、本地模型和 JSONL 输出链路。
- 尚未实现 Python 包、CLI、Worker 或 Verifier。
- 尚未建立 Git 提交；当前记录只描述文档初始化，不代表产品功能完成。

## 下一步

完成 Gate 0：在一个临时测试 Git 仓库中运行最小 Codex/Ollama Worker，记录命令、版本、退出码、事件输出和超时行为。
